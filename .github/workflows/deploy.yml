name: 📰 deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

  concurrency:
   group; ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs: 
  deploy:
    name: 🎪 Deploy to Github pages
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/mean'
    outputs:
    page_url: ${{ steps.deployment.outpouts.page.url }}
